import 'package:flutter/material.dart';

class Break {
  static bool enabled = true;
  static TimeOfDay beginTime = TimeOfDay(hour: 13, minute: 00);
  static TimeOfDay endTime = TimeOfDay(hour: 14, minute: 00);
}
